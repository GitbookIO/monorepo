build:
  main: cmd/monorepo/main.go
  binary: monorepo
  goos:
    - darwin
    - linux
  goarch:
    - amd64
archive:
  name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
  format: binary
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^refactor'
