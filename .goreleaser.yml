build:
  main: cmd/monorepo/main.go
  binary: monorepo
  goos:
    - darwin
    - linux
    - windows
  goarch:
    - amd64
    - 386
archive:
  format: zip
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^refactor'
